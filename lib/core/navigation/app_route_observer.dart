import 'package:flutter/widgets.dart';

/// Global route observer to track navigation changes across the app.
final RouteObserver<ModalRoute<dynamic>> appRouteObserver =
    RouteObserver<ModalRoute<dynamic>>();

